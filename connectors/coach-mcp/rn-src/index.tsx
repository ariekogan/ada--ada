/**
 * coach-mcp — RN Bundle Entry Point
 * Re-exports all plugins so the platform can load them from a single bundle.
 */
export { default as CoachDashboard } from './coach-dashboard';
export { default as CoachOnboarding } from './coach-onboarding';
